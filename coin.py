import random
def BiasedCoinToss(p):
Input the number of trials, N
For i in range of N :
	if(math.rand()<=p)
		return Head
		Print ‘p’=count (Head)/N
	else
		return Tail
		print ‘(1-p)’ = count(Tail)/N
